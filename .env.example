# Neon Postgres connection strings.
# DATABASE_URL: pooled connection (host contains "-pooler"), used by the app at runtime.
# DIRECT_URL: direct connection (no "-pooler"), used by Prisma for migrations.
DATABASE_URL=
DIRECT_URL=
